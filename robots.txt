# training.forrest.one — robots.txt

# NOTE ON THE DUPLICATE BLOCK ABOVE
# Cloudflare injects its own "content signals" robots.txt preamble ahead of this
# file, and it is not editable from the repo. That injected block already
# contains: Content-Signal: search=yes,ai-train=no,use=reference, Allow: / and
# Disallow: / for Amazonbot, Applebot-Extended, Bytespider, CCBot, ClaudeBot,
# CloudflareBrowserRenderingCrawler, Google-Extended, GPTBot and
# meta-externalagent.
#
# robots.txt does NOT merge duplicate groups: when a crawler appears in more than
# one group, implementations keep the FIRST matching group and discard later
# ones. So the Cloudflare block wins for every agent it names, and any rule I
# write for those agents here is ignored.
#
# Consequently this file only carries rules Cloudflare does NOT already set,
# which is why it is short. To change the AI-crawler policy, edit it in the
# Cloudflare dashboard (AI Crawl Control), not here.

# ── Search engines Cloudflare does not name: full access ────────────────────
# (Googlebot/Bingbot are not in the injected block, so these apply.)
User-agent: Googlebot
Allow: /

User-agent: Bingbot
Allow: /

User-agent: DuckDuckBot
Allow: /

User-agent: PerplexityBot
Allow: /

# ── Catch-all for everything else ───────────────────────────────────────────
User-agent: *
Allow: /

# ── Never wanted ────────────────────────────────────────────────────────────
User-agent: SemrushBot
Disallow: /

User-agent: AhrefsBot
Disallow: /

User-agent: MJ12bot
Disallow: /

# ── Sitemap ─────────────────────────────────────────────────────────────────
Sitemap: https://training.forrest.one/sitemap.xml
